package lab3.com.company.neophite;

public class ConnectionPoolHolder {
    public static ConnectionPoll  createPool(String url,String name,String passwd){
        return createPool(url,name,passwd);
    }
}
